name: Supabase Günlük Yedekleme

on:
  schedule:
    - cron: '0 21 * * *' # Her gün TSİ 00:00 (UTC 21:00)
  workflow_dispatch: # Manuel tetikleme için

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install PostgreSQL 17 and Networking Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y wget gnupg socat
          wget --quiet -O - https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo apt-key add -
          echo "deb http://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" | sudo tee /etc/apt/sources.list.d/pgdg.list
          sudo apt-get update
          sudo apt-get install -y postgresql-client-17

      - name: Set Timestamp
        id: timestamp
        run: echo "NOW=$(date +'%Y-%m-%d_%H-%M-%S')" >> $GITHUB_ENV

      - name: Create IPv4 to IPv6 Tunnel
        run: |
          socat TCP4-LISTEN:5432,fork,reuseaddr TCP6:db.jgonjcfxeaigsaokpqoz.supabase.co:5432 &
          sleep 3

      - name: Backup Database
        run: |
          export PGPASSWORD="${{ secrets.DB_PASSWORD }}"

          pg_dump "postgresql://postgres.jgonjcfxeaigsaokpqoz@127.0.0.1:5432/postgres?sslmode=require" \
                  --no-password \
                  -F c \
                  -b \
                  -v > backup_${{ env.NOW }}.dump

      - name: Upload Backup as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: supabase-backup-${{ env.NOW }}
          path: backup_${{ env.NOW }}.dump
          retention-days: 30